const form = document.querySelector("#setup-form");
const errorMessage = document.querySelector("#setup-error");
const errorText = document.querySelector("#setup-error-text");
const languageButton = document.querySelector("#login-language");
let language = localStorage.getItem("waggle-language") || "tr";
let errorDismissTimer = null;
const copy = {
  tr: {eyebrow:"GÜVENLİ İLK KURULUM",tagline:"Kovan sisteminiz size ait.<br>İlk sahibi güvenle oluşturun.",private:"Yerel hesap",protected:"Güvenli parola",one_time:"Tek seferlik kurulum",ready:"Kuruluma hazır",title:"Sistem sahibi oluşturun",hint:"Bu hesap kovanlarınızı ve cihazlarınızı yönetecek.",name:"Adınız",username:"Kullanıcı adı",password:"Parola",confirm:"Parolayı doğrulayın",create:"Sistem sahibi hesabını oluştur",note:"Parolanız yalnızca bu cihazdaki yerel Waggle veritabanında güvenli bir özet olarak saklanır.",back:"← Girişe dön",mismatch:"Parolalar eşleşmiyor",creating:"Hesap oluşturuluyor…",setup_error:"Hesap oluşturulamadı"},
  en: {eyebrow:"SECURE FIRST-TIME SETUP",tagline:"Your hive system belongs to you.<br>Create its first owner securely.",private:"Local account",protected:"Protected password",one_time:"One-time setup",ready:"Ready to set up",title:"Create the system owner",hint:"This account will manage your hives and devices.",name:"Your name",username:"Username",password:"Password",confirm:"Confirm password",create:"Create owner account",note:"Your password is stored only as a secure hash in this device's local Waggle database.",back:"← Back to sign in",mismatch:"Passwords do not match",creating:"Creating account…",setup_error:"Account could not be created"},
};
function clearSetupError(){if(errorDismissTimer)clearTimeout(errorDismissTimer);errorDismissTimer=null;errorMessage.classList.remove("show","fade-out");errorMessage.hidden=true;errorText.textContent="";}
function showSetupError(message){clearSetupError();errorText.textContent=message;errorMessage.hidden=false;errorMessage.classList.add("show");form.querySelectorAll(".input-shell").forEach(field=>field.classList.add("input-error"));errorDismissTimer=setTimeout(()=>{errorMessage.classList.add("fade-out");errorMessage.addEventListener("animationend",clearSetupError,{once:true});},4200);}
function translate() { document.documentElement.lang=language; document.querySelectorAll("[data-i18n]").forEach((el)=>{el.innerHTML=copy[language][el.dataset.i18n];}); languageButton.textContent=language==="tr"?"EN":"TR"; document.title=language==="tr"?"Waggle | İlk Kurulum":"Waggle | First-time setup"; }
languageButton.addEventListener("click",()=>{language=language==="tr"?"en":"tr";localStorage.setItem("waggle-language",language);translate();});
form.addEventListener("submit",async(event)=>{event.preventDefault();clearSetupError();if(form.querySelector("#password").value!==form.querySelector("#password-confirm").value){showSetupError(copy[language].mismatch);return;}const button=form.querySelector("button[type='submit']");button.disabled=true;button.textContent=copy[language].creating;try{const response=await fetch("/api/setup",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({display_name:form.querySelector("#display-name").value.trim(),username:form.querySelector("#username").value.trim(),password:form.querySelector("#password").value})});if(!response.ok){const body=await response.json();throw new Error(body.detail||"Setup failed");}window.location.replace("/");}catch(error){showSetupError(error.message);button.disabled=false;button.textContent=copy[language].create;}});
form.querySelectorAll("input").forEach(input=>input.addEventListener("input",()=>{clearSetupError();input.closest(".input-shell")?.classList.remove("input-error");}));
translate();
